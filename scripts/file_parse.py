#!/usr/bin/env python3
import re
import sys
import pathlib
import subprocess
import convert

path = pathlib.Path(__file__).parent.absolute()
path = str(path.parents[0])


def parse_file(in_encoding, out_encoding, in_fl):

    in_file = open(in_file)


if __name__ == "__main__":

    in_enc = sys.argv[1]
    out_enc = sys.argv[2]
    in_fl = sys.argv[3]
